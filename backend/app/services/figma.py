class FigmaConnector:
    """Placeholder for future Figma design context extraction."""

    def extract_design_context(self, links: list[str]) -> dict:
        return {
            "enabled": bool(links),
            "links": links,
            "screens": [],
        }

